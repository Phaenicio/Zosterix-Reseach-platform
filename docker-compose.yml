services:
  #Uncomment this section to use docker in postgres. Otherwise there will be a port conflict for port port 5432
  #postgres:
  #  image: postgres:15-alpine
  #  container_name: zosterix_postgres
  #  environment:
  #    POSTGRES_USER: postgres
  #    POSTGRES_PASSWORD: 123456
  #    POSTGRES_DB: Zosterix_Local
  #  ports:
  #    - "5432:5432"
  #  volumes:
  #    - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: zosterix_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
